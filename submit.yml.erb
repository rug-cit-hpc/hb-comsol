---
batch_connect:
  template: "vnc"
script:
  queue_name: <%= custom_queue.blank? ? "short" : custom_queue.strip %>
  native:
    - "-c"
    - "<%= num_cores.to_i %>"
    - "-t"
    - "<%= wall_time %>"
